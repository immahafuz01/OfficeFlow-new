{
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "app/build/web",
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter && flutter/bin/flutter pub get --directory app",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
